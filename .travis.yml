language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"

# command to install dependencies
install: 
  - pip install $DJANGO_VERSION xhtml2pdf

# command to run tests
script: python manage.py test

env:
  - DJANGO_VERSION=1.8.16
  - DJANGO_VERSION=1.9.11
  - DJANGO_VERSION=1.10.3

